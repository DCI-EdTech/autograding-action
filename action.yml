name: 'CodeBuddy Action'
description: 'Setup and test'
inputs:
  token:
    description: 'token'
    required: false
    default: ${{ github.token }}
outputs:
  GitHubClassroomWorkflow:
    description: "Points"
    value: ${{ steps.autograder.outputs.Points }}
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: Autograding
      uses: DCI-EdTech/autograding@codebuddy
      id: autograder
      with:
        token: ${{ inputs.token }}
