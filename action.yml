name: 'Autograding'
description: 'Setup and test'
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - uses: DCI-EdTech/autograding@main
      id: autograder
